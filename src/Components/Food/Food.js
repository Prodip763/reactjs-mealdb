import React from 'react';

const Food = (props) => {
    // const {foods} =props;
    console.log(props);
    // const {strMeal, strCategory, strInstructions, strMealThumb} = props.food;
    return (
        <div>
            <h1> Our Food</h1>
        </div>
    );
};

export default Food;